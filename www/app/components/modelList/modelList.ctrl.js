;(function () {
    'use strict';

    var dependencies = [
        'app.modelList'
    ];

    var app = angular.module('app.modelList.ctrl', dependencies);

    app.controller('ModelListCtrl', ['$rootScope','$scope','$state', function ($rootScope, $scope, $state) {

    }]);

}());